@model IEnumerable<WebApplication1.Inventions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.resume)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.resume)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Num_invention }) |
            @Html.ActionLink("Details", "Details", new { id=item.Num_invention }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Num_invention })
        </td>
    </tr>
}

</table>
