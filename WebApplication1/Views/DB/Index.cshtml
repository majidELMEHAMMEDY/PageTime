@model IEnumerable<WebApplication1.Models.Brevet>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date_depot)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date_validation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date_depot)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date_validation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.num_brevet }) |
            @Html.ActionLink("Details", "Details", new { id=item.num_brevet }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.num_brevet })
        </td>
    </tr>
}

</table>
